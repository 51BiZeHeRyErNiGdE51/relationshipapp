import SwiftUI
import WidgetKit

@main
struct LovioWidgetsBundle: WidgetBundle {
    var body: some Widget {
        LoveDaysWidget()
        PolaroidWidget()
        LovePulseWidget()
        OpenQuestionWidget()
        MoodSyncWidget()
        NextAdventureWidget()
        MissYouWidget()
        SecretMessageWidget()
        LoveJarWidget()
        HugMeterWidget()
        CompanionWidget()
        CountdownLiveActivity()
    }
}
