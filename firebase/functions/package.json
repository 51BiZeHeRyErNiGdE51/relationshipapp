{
  "name": "lovio-functions",
  "private": true,
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions --project lovio-18416 && node ../scripts/fix-invokers.mjs",
    "fix-invokers": "node ../scripts/fix-invokers.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
